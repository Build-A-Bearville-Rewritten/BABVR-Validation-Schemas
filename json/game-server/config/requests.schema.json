{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Requests Config",
    "description": "Configuration for the game's request limits",
    "type": "object",
    "properties": {
        "maximumExtensionRequests": {
            "description": "Maximum number of extension requests allowed in a single chunk",
            "type": "integer",
            "minimum": 1
        },
        "maximumExtensionRequestParameterLength": {
            "description": "Maximum length allowed for a single extension request parameter",
            "type": "integer",
            "minimum": 1
        },
        "maximumSocketDataLength": {
            "description": "Maximum length allowed for incoming socket data",
            "type": "integer",
            "minimum": 1
        },
        "maximumXmlRequestLength": {
            "description": "Maximum length allowed for an XML request",
            "type": "integer",
            "minimum": 1
        }
    },
    "additionalProperties": false
}
